using System;

/// <summary>
/// 
/// </summary>
class Player
{
    /// interface
    /// <summary>
    /// The player has to be able to register with the game engine,
    /// contract: the player should not be previously registered to the game engine
    ///           direction should be in the directions dictionary
    /// </summary>
    /// <returns>
    /// A boolean whether a cell adjacent to the worker 
    /// exists in the given direction
    /// </returns>
    public void RegisterPlayer()

    // send to the game engine its next play and
    public void DeclarePlayerMove(){return;}

    // receive a notification upon the end of the game about which player won.

    // dependencies 
    // knowledge

}
